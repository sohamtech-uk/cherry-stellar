{
  "name": "cherry-stellar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "test:live": "node scripts/live.mjs --testnet"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "17.0.1"
  }
}
